import { ButtonProps, ComponentsVariants } from '@mui/material';

/**
 * Styles for text Button
 */
export declare const getButtonTextVariant: (colorName: ButtonProps['color'], variantColor: string, surfaceColor: string) => NonNullable<ComponentsVariants['MuiButton']>[number];
